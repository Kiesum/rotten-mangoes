<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a href="/">
        <img src="http://i.istockimg.com/file_thumbview_approve/23619544/3/stock-illustration-23619544-yellow-ripe-mango.jpg"  style="height:70px;">
      </a>
    </div>
    <div>
    <p class="navbar-text navbar-right">
      <% if current_user %>
          Signed in as <%= current_user.full_name %> <%= link_to "Log out", session_path("current"), method: :delete , :class => 'btn btn-default' %>
          <% if current_user.admin %>
            <%= link_to "Admin page", admin_users_path, :class => 'btn btn-default' %>
          <% end %>
      <% else %>
          <%= link_to "Log In", new_session_path %> | <%= link_to "Sign Up", new_user_path %>
      <% end %>
    </p>
    </div>
  </div>
</nav>
      